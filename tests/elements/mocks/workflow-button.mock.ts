import { WorkflowButtonBuilder } from '../../../src/elements/workflow-button';

export const params = {
    accessibilityLabel: 'Run workflow button',
    text: 'Run Workflow',
};

export const mock = new WorkflowButtonBuilder(params);

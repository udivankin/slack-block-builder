import { RichTextInputBuilder } from '../../../src/elements/rich-text-input';

export const params = {
    actionId: 'actionId',
    placeholder: 'Enter rich text...',
};

export const mock = new RichTextInputBuilder(params);

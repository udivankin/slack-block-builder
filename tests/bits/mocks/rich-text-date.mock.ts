import { RichTextDateBuilder } from '../../../src/bits/rich-text-date';

export const params = {
    timestamp: 1628633820,
    format: '{date_long}',
};

export const mock = new RichTextDateBuilder(params);
